# Telegram bot token for Alpha's current bot entrypoint in src/main.js
ALPHA_BOT_TOKEN=123456:ABC-DEF...

# Main LLM provider for Alpha's current reply path in src/alpha.js
GROQ_API_KEY=gsk_...

# Optional: only used by src/openai.js for audio transcription / legacy OpenAI helpers
OPENAI_KEY=sk-proj-...

# Optional runtime port for Render or local hosting
PORT=3000
